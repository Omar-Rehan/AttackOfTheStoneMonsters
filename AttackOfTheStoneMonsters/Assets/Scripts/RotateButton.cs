using System.Collections;
using UnityEngine;

public class RotateButton : MonoBehaviour {
    // Other game objects
    public GameObject player;

    void Update() {
        ///gameObject.transform.forward = Vector3.Normalize(player.transform.position - transform.position);
    }
}
